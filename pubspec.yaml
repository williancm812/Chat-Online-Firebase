name: chat_online_firebase
description: Setimo projeto do curso da Udemy do Daniel Ciolfi.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  cloud_firestore: ^0.14.4
  image_picker: ^0.6.2+3
  google_sign_in: ^4.5.6
  firebase_storage: ^5.2.0
  firebase_auth: ^0.18.4+1
  firebase_core: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true